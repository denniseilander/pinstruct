name: Probe
on: workflow_dispatch
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  probe:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - run: echo ok
